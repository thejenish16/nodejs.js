<!DOCTYPE html>
<html lang="en">

<head>
    <title>Form</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            width: 100%;
            height: 100%;
            margin: 0;
            box-sizing: border-box;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            width: 100%;
        }

        table {
            border-collapse: collapse;
            width: 500px;
            background: #fafafa;
            padding: 20px;
            border-radius: 10px;
        }

        td {
            padding: 12px 15px;
            font-size: 16px;
        }

        input,
        textarea {
            width: 100%;
            padding: 10px 12px;
            font-size: 16px;
            border: 1px solid #c9c9c9;
            border-radius: 6px;
            box-sizing: border-box;
        }

        textarea {
            height: 70px;
            resize: none;
        }

        th,
        td {
            border: none;
        }

        button,
        input[type="submit"] {
            padding: 10px 20px;
            background: #ffffff;
            border: 2px solid #dcdcdc;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.2s ease;
            box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.08);
        }

        button:hover,
        input[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0px 5px 12px rgba(0, 0, 0, 0.12);
        }

        button:active,
        input[type="submit"]:active {
            background: #efefefe0;
            transform: translateY(1px);
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.10);
        }

        a {
            display: block;
            text-align: center;
            margin-top: 20px;
            font-size: 18px;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <h1 align="center">Form (CRUD Operation)</h1>

    <!-- Add User  -->
    <div class="container">

        <!-- LEFT FORM -->
        <form method="POST" action="/addUser">
            <table cellspacing="5" cellpadding="5">
                <tr>
                    <td>Name : </td>
                    <td><input type="text" name="Name" required placeholder="Enter Your Name.."></td>
                </tr>
                <tr>
                    <td>Email : </td>
                    <td><input type="email" name="Email" required placeholder="Enter Your Email.."></td>
                </tr>
                <tr>
                    <td>Password : </td>
                    <td><input type="password" name="Password" required placeholder="Enter Your Password.."></td>
                </tr>
                <tr>
                    <td>Phone : </td>
                    <td><input type="number" name="Phone" required placeholder="Enter Your Phone Number.."></td>
                </tr>
                <tr>
                    <td>Address : </td>
                    <td><textarea name="Address" required placeholder="Enter Your Address..."></textarea></td>
                </tr>
                <tr>
                    <td>Age : </td>
                    <td><input type="number" name="Age" required placeholder="Enter Your Age.."></td>
                </tr>
                <tr>
                    <td>Add</td>
                    <td><input type="submit" value="Add"></td>
                </tr>
            </table>
        </form>


    </div>
    <a href="/addUserPage">GoTo Table</a>
</body>

</html>