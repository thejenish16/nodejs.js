<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JioHotstar | Watch Movies & TV Shows</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #030b17;
            --surface-color: #0f172a;
            --accent-color: #0078ff;
            --text-primary: #ffffff;
            --text-secondary: #94a3b8;
            --glass: rgba(15, 23, 42, 0.8);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- Navbar --- */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1.2rem 5%;
            z-index: 1000;
            background: linear-gradient(to bottom, rgba(3, 11, 23, 0.9), transparent);
            backdrop-filter: blur(10px);
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: 0.3s ease;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            letter-spacing: -1px;
            color: var(--text-primary);
            text-decoration: none;
            background: linear-gradient(45deg, #0078ff, #00d4ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-links .nav-btn {
            background: var(--accent-color);
            color: white;
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: 0.3s;
        }

        .nav-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 120, 255, 0.4);
        }

        /* --- Hero Section --- */
        .hero {
            height: 80vh;
            position: relative;
            display: flex;
            align-items: center;
            padding: 0 5%;
            background: linear-gradient(to right, var(--bg-color) 20%, transparent),
                url('https://images.unsplash.com/photo-1626814026160-2237a95fc5a0?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80');
            background-size: cover;
            background-position: center;
        }

        .hero-content {
            max-width: 600px;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 1rem;
        }

        .hero-stats {
            display: flex;
            gap: 2rem;
            margin-top: 1.5rem;
        }

        .stat-item .stat-number {
            display: block;
            font-size: 1.2rem;
            font-weight: 700;
        }

        .stat-item .stat-label {
            color: var(--text-secondary);
            font-size: 0.8rem;
        }

        /* --- Video Grid --- */
        .main-container {
            padding: 3rem 5%;
        }

        .section-title {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            font-weight: 600;
        }

        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
            gap: 2rem;
        }

        .video-card {
            background: var(--surface-color);
            border-radius: 12px;
            overflow: hidden;
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 1px solid rgba(255, 255, 255, 0.05);
            position: relative;
        }

        .video-card:hover {
            transform: scale(1.05);
            z-index: 10;
            border-color: var(--accent-color);
        }

        .video-thumbnail {
            width: 100%;
            aspect-ratio: 16/9;
            object-fit: cover;
        }

        .video-info {
            padding: 1.2rem;
        }

        .video-title {
            font-size: 1rem;
            margin-bottom: 0.5rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .video-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.75rem;
            color: var(--text-secondary);
            margin-bottom: 1rem;
        }

        .category-badge {
            background: rgba(255, 255, 255, 0.1);
            padding: 2px 8px;
            border-radius: 4px;
            text-transform: uppercase;
        }

        /* --- Actions --- */
        .video-actions {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 0.5rem;
        }

        .btn {
            padding: 0.5rem;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.8rem;
            text-align: center;
            font-weight: 600;
            transition: 0.2s;
        }

        .btn-primary {
            background: var(--accent-color);
            color: white;
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .btn-danger {
            background: rgba(255, 68, 68, 0.1);
            color: #ff4444;
        }

        .btn-danger:hover {
            background: #ff4444;
            color: white;
        }

        .empty-state {
            text-align: center;
            padding: 5rem 0;
            color: var(--text-secondary);
        }

        @media (max-width: 768px) {
            .hero-content h1 {
                font-size: 2.5rem;
            }

            .navbar {
                padding: 1rem 5%;
            }
        }
    </style>
</head>

<body>

    <nav class="navbar" id="navbar">
        <a href="/" class="logo">JioHotstar</a>
        <div class="nav-links">
            <a href="/add-video" class="nav-btn">Add Content</a>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-content">
            <h1>Unlimited Movies, TV Shows & More</h1>
            <p style="color: var(--text-secondary); margin-bottom: 2rem;">Watch anywhere. Cancel anytime. Experience the
                next generation of Indian entertainment.</p>
            <div class="hero-stats">
                <div class="stat-item">
                    <span class="stat-number">10K+</span>
                    <span class="stat-label">Titles</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">4K</span>
                    <span class="stat-label">Quality</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">Free</span>
                    <span class="stat-label">Trial</span>
                </div>
            </div>
        </div>
    </section>

    <main class="main-container">
        <h2 class="section-title">Trending Now</h2>

        <% if (videos && videos.length> 0) { %>
            <div class="video-grid">
                <% videos.forEach(video=> { %>
                    <div class="video-card">
                        <img src="<%= video.thumbnail %>" alt="<%= video.title %>" class="video-thumbnail">
                        <div class="video-info">
                            <h3 class="video-title">
                                <%= video.title %>
                            </h3>
                            <div class="video-meta">
                                <span class="category-badge">
                                    <%= video.category %>
                                </span>
                                <span class="duration">⏱️ <%= video.duration %></span>
                            </div>
                            <div class="video-actions">
                                <a href="/watch/<%= video._id %>" class="btn btn-primary">Watch Now</a>
                                <a href="/edit-video/<%= video._id %>" class="btn btn-secondary">Edit</a>
                                <a href="/delete-video/<%= video._id %>" class="btn btn-danger">Delete</a>
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <div class="empty-state">
                    <h2 style="color: white; margin-bottom: 1rem;">Your library is empty</h2>
                    <p>Start building your cinematic collection today.</p>
                    <br>
                    <a href="/add-video" class="nav-btn">Upload Your First Video</a>
                </div>
                <% } %>
    </main>

    <script>
        // Navbar scroll effect
        window.addEventListener('scroll', () => {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'var(--bg-color)';
                navbar.style.padding = '0.8rem 5%';
            } else {
                navbar.style.background = 'linear-gradient(to bottom, rgba(3, 11, 23, 0.9), transparent)';
                navbar.style.padding = '1.2rem 5%';
            }
        });
    </script>
</body>

</html>