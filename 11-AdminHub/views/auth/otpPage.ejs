<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification - AdminHub</title>
    <link rel="stylesheet" href="/css/app.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="auth-container">
        <div class="auth-card fade-in">
            <div class="text-center mb-3">
                <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; box-shadow: var(--shadow-lg);">
                    <i class="fas fa-shield-check" style="font-size: 32px; color: white;"></i>
                </div>
                <h1 class="auth-title">Enter OTP</h1>
                <p style="color: var(--gray); font-weight: 500;">Check your email for verification code</p>
            </div>
            
            <form action="/verify-otp" method="POST">
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-key" style="color: var(--primary); margin-right: 8px;"></i>
                        Enter OTP
                    </label>
                    <input type="text" name="OTP" class="form-input" placeholder="Enter 6-digit OTP" maxlength="6" required style="text-align: center; font-size: 24px; letter-spacing: 8px;">
                </div>
                
                <button type="submit" class="btn btn-primary w-full" style="padding: 16px; font-size: 16px; font-weight: 700;">
                    <i class="fas fa-check"></i>
                    Verify OTP
                </button>
            </form>
            
            <div class="text-center mt-3">
                <div style="height: 1px; background: var(--border); margin: 24px 0;"></div>
                <p style="color: var(--gray); font-size: 14px;">
                    <i class="fas fa-clock" style="color: var(--warning); margin-right: 8px;"></i>
                    OTP expires in 10 minutes
                </p>
            </div>
        </div>
    </div>
</body>
</html>