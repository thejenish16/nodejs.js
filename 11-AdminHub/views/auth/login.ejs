<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - AdminHub</title>
    <link rel="stylesheet" href="/css/app.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="auth-container">
        <div class="auth-card fade-in">
            <div class="text-center mb-3">
                <div style="width: 80px; height: 80px; background: linear-gradient(135deg, var(--primary), var(--primary-dark)); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; box-shadow: var(--shadow-lg);">
                    <i class="fas fa-shield-alt" style="font-size: 32px; color: white;"></i>
                </div>
                <h1 class="auth-title">Welcome Back</h1>
                <p style="color: var(--gray); font-weight: 500;">Sign in to your AdminHub account</p>
            </div>
            
            <form action="/login" method="POST">
                <% if (typeof success !== 'undefined') { %>
                    <div style="background: #efe; border: 1px solid #cfc; color: #363; padding: 12px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
                        <i class="fas fa-check-circle"></i> <%= success %>
                    </div>
                <% } %>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-envelope" style="color: var(--primary); margin-right: 8px;"></i>
                        Email Address
                    </label>
                    <input type="email" name="email" class="form-input" placeholder="Enter your email address" required>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-lock" style="color: var(--primary); margin-right: 8px;"></i>
                        Password
                    </label>
                    <input type="password" name="password" class="form-input" placeholder="Enter your password" required>
                </div>
                
                <button type="submit" class="btn btn-primary w-full" style="padding: 16px; font-size: 16px; font-weight: 700;">
                    <i class="fas fa-sign-in-alt"></i>
                    Sign In to AdminHub
                </button>
            </form>
            
            <div class="text-center mt-3">
                <div style="height: 1px; background: var(--border); margin: 24px 0;"></div>
                <a href="/forgot-password" style="color: var(--primary); text-decoration: none; font-size: 14px; font-weight: 600;">
                    <i class="fas fa-key" style="margin-right: 4px;"></i>
                    Forgot Password?
                </a>
                <p style="color: var(--gray); font-size: 14px; font-weight: 500; margin-top: 16px;">
                    <i class="fas fa-shield-check" style="color: var(--success); margin-right: 8px;"></i>
                    Secure Admin Access
                </p>
            </div>
        </div>
    </div>
</body>
</html>