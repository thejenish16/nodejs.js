<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password - AdminHub</title>
    <link rel="stylesheet" href="/css/app.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="auth-container">
        <div class="auth-card fade-in">
            <div class="text-center mb-3">
                <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #28a745 0%, #20c997 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; box-shadow: var(--shadow-lg);">
                    <i class="fas fa-lock" style="font-size: 32px; color: white;"></i>
                </div>
                <h1 class="auth-title">Set New Password</h1>
                <p style="color: var(--gray); font-weight: 500;">Create a strong password for your account</p>
            </div>
            
            <form action="/forgot-password" method="POST">
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-key" style="color: var(--primary); margin-right: 8px;"></i>
                        New Password
                    </label>
                    <input type="password" name="newPass" class="form-input" placeholder="Enter new password" required minlength="6">
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-check-circle" style="color: var(--primary); margin-right: 8px;"></i>
                        Confirm Password
                    </label>
                    <input type="password" name="ConfPass" class="form-input" placeholder="Confirm new password" required minlength="6">
                </div>
                
                <button type="submit" class="btn btn-primary w-full" style="padding: 16px; font-size: 16px; font-weight: 700;">
                    <i class="fas fa-save"></i>
                    Update Password
                </button>
            </form>
            
            <div class="text-center mt-3">
                <div style="height: 1px; background: var(--border); margin: 24px 0;"></div>
                <p style="color: var(--gray); font-size: 14px;">
                    <i class="fas fa-shield-alt" style="color: var(--success); margin-right: 8px;"></i>
                    Your account will be secured with the new password
                </p>
            </div>
        </div>
    </div>
</body>
</html>