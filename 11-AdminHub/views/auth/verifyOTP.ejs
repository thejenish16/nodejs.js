<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify OTP - AdminHub</title>
    <link rel="stylesheet" href="/css/app.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="auth-container">
        <div class="auth-card fade-in">
            <div class="text-center mb-3">
                <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; box-shadow: var(--shadow-lg);">
                    <i class="fas fa-shield-check" style="font-size: 32px; color: white;"></i>
                </div>
                <h1 class="auth-title">Verify OTP</h1>
                <p style="color: var(--gray); font-weight: 500;">Enter OTP sent to your email</p>
            </div>
            
            <% if (typeof success !== 'undefined') { %>
                <div style="background: #efe; border: 1px solid #cfc; color: #363; padding: 12px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
                    <i class="fas fa-check-circle"></i> <%= success %>
                </div>
            <% } %>
            
            <% if (typeof error !== 'undefined') { %>
                <div style="background: #fee; border: 1px solid #fcc; color: #c33; padding: 12px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
                    <i class="fas fa-exclamation-triangle"></i> <%= error %>
                </div>
            <% } %>
            
            <form action="/verifyOTP" method="POST">
                <input type="hidden" name="email" value="<%= email %>">
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-key" style="color: var(--primary); margin-right: 8px;"></i>
                        Enter OTP
                    </label>
                    <input type="text" name="otp" class="form-input" placeholder="Enter 6-digit OTP" maxlength="6" pattern="[0-9]{6}" required style="text-align: center; font-size: 24px; letter-spacing: 8px;">
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-lock" style="color: var(--primary); margin-right: 8px;"></i>
                        New Password
                    </label>
                    <input type="password" name="newPassword" class="form-input" placeholder="Enter new password" required minlength="6">
                </div>
                
                <button type="submit" class="btn btn-primary w-full" style="padding: 16px; font-size: 16px; font-weight: 700;">
                    <i class="fas fa-check"></i>
                    Reset Password
                </button>
            </form>
            
            <div class="text-center mt-3">
                <div style="height: 1px; background: var(--border); margin: 24px 0;"></div>
                <p style="color: var(--gray); font-size: 14px;">
                    <i class="fas fa-clock" style="color: var(--warning); margin-right: 8px;"></i>
                    OTP expires in 10 minutes
                </p>
                <a href="/forgotPassword" style="color: var(--primary); text-decoration: none; font-weight: 600;">
                    <i class="fas fa-redo" style="margin-right: 8px;"></i>
                    Resend OTP
                </a>
            </div>
        </div>
    </div>
</body>
</html>